/**
 * Achievement Manager Service\n * Manages achievement tracking and unlocking\n * \n * Validates: Requirements 4.1, 4.2, 4.3, 4.7\n */\n\nimport { Achievement, UserAchievement } from '../types';\nimport { DEFAULT_ACHIEVEMENTS } from '../constants';\n\nexport class AchievementManager {\n  private achievements: Map<string, Achievement> = new Map();\n  private userAchievements: Map<string, Set<string>> = new Map(); // userId -> achievementIds\n  private unlockedAt: Map<string, Map<string, string>> = new Map(); // userId -> achievementId -> timestamp\n\n  constructor() {\n    // Initialize with default achievements\n    DEFAULT_ACHIEVEMENTS.forEach((achievement) => {\n      this.achievements.set(achievement.id, achievement);\n    });\n  }\n\n  /**\n   * Define achievements\n   * Requirement 4.2: Define achievements with required fields\n   */\n  defineAchievements(achievements: Achievement[]): void {\n    achievements.forEach((achievement) => {\n      this.achievements.set(ac